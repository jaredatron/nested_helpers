# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/nested_helpers.rb'

Hoe.new('nested_helpers', NestedHelpers::VERSION) do |p|
  # p.rubyforge_name = 'nested_helpersx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
